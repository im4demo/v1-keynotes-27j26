# Browser-facing API URL (no trailing slash).
NEXT_PUBLIC_API_URL=http://localhost:4000

# Optional server-side API URL (used inside Docker). Falls back to NEXT_PUBLIC_API_URL.
# API_URL=http://api:4000
